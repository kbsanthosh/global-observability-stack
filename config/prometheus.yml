global:
  scrape_interval: 15s
  evaluation_interval: 15s

# Enable feature flag for native OpenTelemetry histogram conversions
enable_features:
  - otlp-write-receiver

scrape_configs:
  - job_name: 'prometheus-self'
    static_configs:
      - targets: ['localhost:9090']
