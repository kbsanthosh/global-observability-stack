server:
  http_listen_port: 3200

distributor:
  receivers:
    otlp:
      protocols:
        grpc:
          endpoint: 0.0.0.0:4317

ingester:
  max_block_duration: 5m

compactor:
  compaction:
    compacted_block_size_bytes: 524288000

storage:
  trace:
    backend: local
    local:
      path: /tmp/tempo/blocks
    # Enforces your exact 24h retention window and 512MB threshold natively
    retention:
      retention_period: 24h
      max_bytes: 536870912
